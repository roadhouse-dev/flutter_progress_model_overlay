# flutter_progress_model_overlay

A simple widget wrapper to enable modal progress overlay (a modal progress indicator)

## How to use

```dart
      ProgressOverlayIndicator(
        showProgressIndicator: true,
        child: 'Widget',
      )
 ```  
